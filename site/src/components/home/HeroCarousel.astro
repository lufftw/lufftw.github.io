---
/**
 * HeroCarousel
 *
 * Featured-only hero carousel using horizontal scroll-snap.
 *
 * Features:
 * - Prev / Next buttons
 * - Dots indicator
 * - Slide counter (e.g. 1 / 5)
 *
 * Implementation notes:
 * - No dynamic attributes inside HTML tags that may confuse Astro parser
 * - No external libraries
 */

import { getPathFromEntry } from "@/utils/getPath";

const { items } = Astro.props;

// Resolve hero image source from frontmatter
function coverOf(entry) {
  return entry.data?.heroImage || entry.data?.coverImage || entry.data?.image;
}
---

<section class="mt-4" data-hero-root>
  <div class="relative overflow-hidden rounded-2xl border border-slate-200">
    <!-- Scrollable carousel container -->
    <div
      class="flex snap-x snap-mandatory overflow-x-auto scroll-smooth"
      style="scrollbar-width: none;"
      data-carousel
      aria-label="Featured posts carousel"
    >
      {items.map((entry, idx) => (
        <article
          class="relative h-[320px] w-full flex-none snap-start md:h-[420px]"
          data-slide
          data-index={idx}
        >
          <a href={getPathFromEntry(entry)} class="block h-full w-full">
            {coverOf(entry) ? (
              idx === 0 ? (
                <img
                  src={coverOf(entry)}
                  alt={entry.data?.title ?? entry.slug}
                  class="h-full w-full object-cover"
                  loading="eager"
                />
              ) : (
                <img
                  src={coverOf(entry)}
                  alt={entry.data?.title ?? entry.slug}
                  class="h-full w-full object-cover"
                  loading="lazy"
                />
              )
            ) : (
              <div class="h-full w-full bg-slate-100" />
            )}

            <!-- Gradient overlay -->
            <div class="absolute inset-0 bg-gradient-to-t from-black/70 via-black/20 to-transparent" />

            <!-- Text overlay -->
            <div class="absolute bottom-0 left-0 right-0 p-6 text-white">
              <h2 class="text-xl font-semibold leading-snug md:text-3xl">
                {entry.data?.title ?? entry.slug}
              </h2>

              {entry.data?.datetime && (
                <div class="mt-2 text-sm opacity-90">
                  {entry.data.datetime}
                </div>
              )}
            </div>
          </a>
        </article>
      ))}
    </div>

    <!-- Prev / Next controls -->
    <button
      class="absolute left-3 top-1/2 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-sm shadow hover:bg-white"
      data-prev
      aria-label="Previous slide"
    >
      ‹
    </button>

    <button
      class="absolute right-3 top-1/2 -translate-y-1/2 rounded-full bg-white/90 px-3 py-2 text-sm shadow hover:bg-white"
      data-next
      aria-label="Next slide"
    >
      ›
    </button>

    <!-- Bottom bar: dots + counter -->
    <div class="absolute bottom-3 left-0 right-0 flex items-center justify-center gap-4">
      <!-- Dots -->
      <div class="flex items-center gap-2" data-dots aria-label="Slide indicators">
        {items.map((_, idx) => (
          <button
            class="h-2 w-2 rounded-full bg-white/60 hover:bg-white"
            data-dot
            data-dot-index={idx}
            aria-label={`Go to slide ${idx + 1}`}
          />
        ))}
      </div>

      <!-- Counter -->
      <div class="rounded-full bg-black/40 px-3 py-1 text-xs text-white" data-counter>
        <span data-current>1</span> / <span data-total>{items.length}</span>
      </div>
    </div>
  </div>
</section>

<script is:inline>
  /**
   * Carousel controller:
   * - Scroll by container width
   * - Track active slide via IntersectionObserver
   * - Sync dots and counter
   * - Autoplay with pause-on-hover/focus and prefers-reduced-motion support
   */
  const root = document.querySelector("[data-hero-root]");
  const scroller = root?.querySelector("[data-carousel]");
  const prev = root?.querySelector("[data-prev]");
  const next = root?.querySelector("[data-next]");
  const slides = Array.from(root?.querySelectorAll("[data-slide]") ?? []);
  const dots = Array.from(root?.querySelectorAll("[data-dot]") ?? []);
  const currentEl = root?.querySelector("[data-current]");
  const totalEl = root?.querySelector("[data-total]");

  if (root && scroller && slides.length > 0) {
    const total = slides.length;
    if (totalEl) totalEl.textContent = String(total);

    // Track active index in JS state (used by autoplay)
    let activeIndex = 0;

    const setActive = (index) => {
      activeIndex = index;
      if (currentEl) currentEl.textContent = String(index + 1);

      dots.forEach((dot, i) => {
        dot.classList.toggle("bg-white", i === index);
        dot.classList.toggle("bg-white/60", i !== index);
      });
    };

    const scrollToIndex = (index) => {
      const target = slides[index];
      if (!target) return;
      target.scrollIntoView({ behavior: "smooth", inline: "start" });
    };

    const scrollByOne = (direction) => {
      const width = scroller.clientWidth;
      scroller.scrollBy({ left: direction * width, behavior: "smooth" });
    };

    prev?.addEventListener("click", () => scrollByOne(-1));
    next?.addEventListener("click", () => scrollByOne(1));

    dots.forEach((dot) => {
      dot.addEventListener("click", () => {
        const idx = Number(dot.getAttribute("data-dot-index"));
        if (!Number.isNaN(idx)) scrollToIndex(idx);
      });
    });

    const io = new IntersectionObserver(
      (entries) => {
        let best = null;
        for (const e of entries) {
          if (!best || e.intersectionRatio > best.intersectionRatio) best = e;
        }
        if (!best || !best.isIntersecting) return;

        const idx = Number(best.target.getAttribute("data-index"));
        if (!Number.isNaN(idx)) setActive(idx);
      },
      { root: scroller, threshold: [0.4, 0.6, 0.8] }
    );

    slides.forEach((s) => io.observe(s));

    // ---- Autoplay ---------------------------------------------------------
    const AUTOPLAY_MS = 6000;

    // Respect user accessibility preference
    const prefersReducedMotion =
      window.matchMedia &&
      window.matchMedia("(prefers-reduced-motion: reduce)").matches;

    let timer = null;
    let paused = false;

    const stopAutoplay = () => {
      if (timer) {
        clearInterval(timer);
        timer = null;
      }
    };

    const startAutoplay = () => {
      if (prefersReducedMotion) return;
      if (timer) return;

      timer = setInterval(() => {
        if (paused) return;

        // Advance to next slide (wrap around)
        const nextIndex = (activeIndex + 1) % total;
        scrollToIndex(nextIndex);
      }, AUTOPLAY_MS);
    };

    // Pause autoplay when user interacts (hover/focus/touch)
    const pause = () => { paused = true; };
    const resume = () => { paused = false; };

    root.addEventListener("mouseenter", pause);
    root.addEventListener("mouseleave", resume);
    root.addEventListener("focusin", pause);
    root.addEventListener("focusout", resume);

    // Touch: pause while touching, resume shortly after
    root.addEventListener("touchstart", pause, { passive: true });
    root.addEventListener("touchend", () => {
      // Small delay to avoid fighting the user's swipe momentum
      setTimeout(resume, 800);
    }, { passive: true });

    // Start autoplay only if there are at least 2 slides
    if (total >= 2) startAutoplay();

    // ----------------------------------------------------------------------

    // Initialize state
    setActive(0);

    // Ensure cleanup if the page is replaced (Astro client-side navigation)
    document.addEventListener("astro:before-swap", () => {
      stopAutoplay();
      io.disconnect();
    }, { once: true });
  }
</script>
